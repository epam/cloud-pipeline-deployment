{{- if and .Values.enabled .Values.shareSrv.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: cp-hook-script-register-share-in-idp
  namespace: {{ include "lib.application.namespace" . }}
  annotations:
    "helm.sh/hook": post-install
    "helm.sh/hook-weight": "12"
    "helm.sh/hook-delete-policy": hook-succeeded
data:
  register-share-in-idp.sh: |
{{ .Files.Get "files/hooks/register-share-in-idp.sh" | indent 4 }}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: hook-register-share-srv-in-idp
  namespace: {{ include "lib.application.namespace" . }}
  annotations:
    "helm.sh/hook": post-install
    "helm.sh/hook-weight": "13"
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  backoffLimit: 0
  activeDeadlineSeconds: 600
  template:
    spec:
      tolerations:
        - key: node-role.kubernetes.io/control-plane
          operator: Exists
          effect: NoSchedule
        - key: node-role.kubernetes.io/master
          operator: Exists
          effect: NoSchedule
      serviceAccountName: {{ include "lib.hook.serviceaccount" . }}
      restartPolicy: Never
      containers:
        - name: hook-register-share-srv-in-idp
          image: {{ include "lib.hook.deploymentImage" . | quote }}
          imagePullPolicy: IfNotPresent
          env:
            - name: NAMESPACE
              value: {{ include "lib.application.namespace" . | quote }}
            - name: CP_SHARE_SRV_EXTERNAL_HOST
              value: {{ .Values.shareSrv.service.host.external | quote }}
            - name: CP_SHARE_SRV_EXTERNAL_PORT
              value: {{ .Values.shareSrv.service.port.external | quote }}
          command: ["/bin/bash", "/scripts/register-share-in-idp.sh"]
          volumeMounts:
            - name: scripts
              mountPath: /scripts
      volumes:
        - name: scripts
          configMap:
            name: cp-hook-script-register-share-in-idp
            defaultMode: 0755
{{- end }}
